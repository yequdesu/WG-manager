#!/bin/bash
# WG-TUI Launcher — dispatches to the Rust Ratatui dashboard
# Always named /usr/local/bin/wg-tui

if command -v wg-tui-ratatui &>/dev/null; then
    exec wg-tui-ratatui "$@"
fi

echo "wg-tui-ratatui is not installed. Run: cd ~/WG-manager/wg-tui && bash install.sh" >&2
exit 127
